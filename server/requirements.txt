# Common project dependencies.
fastapi>=0.116.0
uvicorn[standard]>=0.35.0
python-multipart>=0.0.20
requests>=2.32.0
numpy>=2.2.0,<3.0.0
pillow>=11.0.0
opencv-python>=4.11.0
transformers>=5.4.0
accelerate>=1.10.0

# PaddleOCR 3.5.0 + PaddleX (engine='transformers' работает без PaddlePaddle)
paddleocr>=3.5.0
paddlex>=3.5.2

# llama-cpp-python для GGUF инференса.
# CPU: pip install llama-cpp-python
# GPU: CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python --no-cache-dir
# Не ставим автоматически — нужна правильная сборка под железо.
